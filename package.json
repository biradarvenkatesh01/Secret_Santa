{
    "name": "secret-santa-rooms",
    "version": "1.0.0",
    "description": "Secret Santa Rooms MVP",
    "scripts": {
        "start": "node server/index.js",
        "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
        "heroku-postbuild": "npm run build"
    },
    "engines": {
        "node": "18.x"
    },
    "dependencies": {
        "express": "^4.18.2",
        "socket.io": "^4.7.2",
        "cors": "^2.8.5"
    }
}